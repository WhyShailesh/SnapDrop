# Optional: Backend URL for LAN or custom server
# Use your machine's local IP for same-WiFi sharing (e.g. http://192.168.1.100:3001)
# Leave empty to use same host (Vite proxy)
# VITE_API_URL=http://localhost:3001
